export enum SiteSettingValueType {
  TEXT = 'text',
  TEXTAREA = 'textarea',
  NUMBER = 'number',
  BOOLEAN = 'boolean',
  URL = 'url',
  IMAGE_URL = 'image_url',
  MEDIA_ASSET = 'media_asset',
  JSON = 'json',
}
